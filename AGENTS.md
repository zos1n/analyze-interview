# 面试录屏分析任务规则 (AGENTS.md)

## 唯一执行依据

开始任何操作前，必须完整读取：
- `docs/面试录屏分析工具规范.md`
- `config/analyzer-config.yaml`

规范是本任务的唯一执行依据，config 提供 venv 路径、解释器、镜像、阈值、词表与模型版本；本文件只提供不可绕过的底线。规范未覆盖且会影响结果时，停止并报告用户，不自行选择替代方案。

## 授权与文件边界

- 未获得用户对目标视频的明确读取授权前，不得访问视频内容或元数据。
- 授权范围仅限只读源视频；禁止覆盖、修改或删除源视频。
- 只允许在规范定义的 `<视频文件名去扩展名>_analysis/` 产物目录中新建文件；唯一例外是共享 venv（config 的 `runtime.venv_path`，仅在缺失且已获安装批准时创建）。目录不可写立即停止，不改换目录。
- 开工前记录源视频 `size + mtime + SHA256` 基线，最终验收时逐项复核一致。
- 不删除、覆盖或修改已生成的中间产物和已验收产物；失败时停止、记录错误并保留现场。
- 临时任务文件、方案改造文档（如 `docs/优化改造方案.md` 等）在执行落地且全部测试/验收通过后，**必须自动移动归档至 `docs/archive/` 目录**（如 `docs/archive/<方案名>.md`），保持 `docs/` 根目录仅保留正式规范与长期维护文档。

## 禁止事项

- 禁止修改 `.env`、密钥、token、证书或项目配置。
- 禁止使用云端视频理解、云端 ASR 或声纹服务。
- 禁止把完整转写稿交给分析模型；分析阶段只允许读取 `transcript-key.md`。
- 禁止从来源不明的位置下载 FFmpeg、Python 包或模型文件。
- 禁止失败后自动切换云端或其他方案，禁止从头重跑已完成区间。

## 稳定环境事实

- 网络状况（代理端口、pypi/HF 直连）不预设结论，一律以规范阶段 0 预检实测为准；curl 不读 macOS 系统代理而 Python httpx/urllib 读，预检必须双测。
- `mlx-whisper` 版本、模型 revision、镜像与阈值统一见 `config/analyzer-config.yaml`，不改用其他版本。
- `--output-format` 不支持逗号分隔（用 `all`）。
- Whisper Turbo 有“嗯”重复循环风险 → 默认 `--condition-on-previous-text False`（连字符形式，mlx-whisper 0.4.3 CLI 不接受下划线），循环出现时只重转坏区间。
- 共享 venv 全机一个（`runtime.venv_path`），不在产物目录内重建。
- 共享 venv 是 Python 3.9：f-string 表达式内不能复用外层引号（`f"{",".join(x)}"` 会 SyntaxError），分隔符字符串先 `join` 成变量再插入 f-string。
- `mlx-whisper` 0.4.3 的 `--clip-timestamps` 多区间有 bug：跨区间 `seek` 不重置，会从首区间起点连续转录到尾区间终点（间隔区全被转录）；多区间必须拆成多条单区间命令分别执行后合并。

## 阶段与验收

严格按“预检 → 安装审批 → 音轨提取 → Small 粗转 → 病理检测 → 问题索引 → Turbo 精转 → 质量校验 → 分析模式选择 → 验收”执行。每个阶段必须先验收该阶段产物，再进入下一阶段；每次阶段完成、决策、失败或恢复都同步更新 `execution-state.json` 与 `execution-log.txt`。安装审批门仅在依赖缺失时触发：先只读核验 FFmpeg、共享 venv 与模型缓存，全部就绪则记录后直接继续；有缺失时一次性列明全部缺失项并等待用户批准。分析器模式 `codex` / `api` / `self` 必须由用户选择。
